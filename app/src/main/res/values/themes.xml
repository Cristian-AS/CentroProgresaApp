<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppCentroProgresa" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Text Colors -->
        <item name="android:textColor">@android:color/black</item>

        <!-- Boton Colors -->
        <item name="colorPrimary">@color/yellowAplicattion</item>

        <!-- Fondo de App -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/yellowAplicattion</item>
    </style>

    <style name="TextViewtxtInfo">
        <item name="android:textColor">@color/letterBox</item>
    </style>

    <!-- Estilo personalizado para el LinearLayout en modo diurno -->
    <style name="LinearLayoutNightStyle">
        <item name="android:background">@drawable/ic_background_blue</item>
    </style>
</resources>