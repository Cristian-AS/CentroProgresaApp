<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.AppCentroProgresa" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Text Colors -->
        <item name="android:textColor">@android:color/black</item>

        <!-- Boton Colors -->
        <item name="colorPrimary">@color/yellowAplicattion</item> <!-- Tener en cuenta para el color de la barra de navegación  -->

        <!-- Fondo de App -->
        <item name="colorOnPrimary">@color/white</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" >@color/yellowAplicattion</item>
    </style>

    // Botón de color para la barra de navegación
    <style name="BottomNavigation">
        <item name="android:background">@color/blueAplicattion</item>
        <item name="itemIconTint">@color/white</item>
        <item name="itemTextColor">@color/white</item>
    </style>

    // Botón de color para la el rectangulo de perfil
    <style name="backgroundPerfil">
        <item name="android:background">@drawable/ic_botton_rectangule_blue</item>
    </style>
</resources>